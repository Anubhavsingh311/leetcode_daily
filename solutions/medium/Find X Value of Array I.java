// Title: Find X Value of Array I
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/find-x-value-of-array-i/

                if (prev[r] > 0) {
            for (int r = 0; r < k; r++) {
            curr[val]++;
            int val = nums[i] % k;
            long[] curr = new long[k];
        for (int i = 0; i < n; i++) {
        long[] prev = new long[k];
        long[] result = new long[k];
        int n = nums.length;
    public long[] resultArray(int[] nums, int k) {
class Solution {
                    curr[(int)((long)r * val % k)] += prev[r];
                }
